# transit-lab

## Quick start

```bash
uv sync
uv run marimo edit
```

## Multi-page app with navbar

Run all notebooks with a shared navigation menu:

```bash
uv run python run_app.py
```

Then open http://127.0.0.1:2718 — use the navbar to switch between **Tracks** and **Lines**.