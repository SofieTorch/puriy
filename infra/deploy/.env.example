# PostgreSQL credentials
POSTGRES_USER=cbba
POSTGRES_PASSWORD=changeme
POSTGRES_DB=cbba_mobility

# Host interface the internal simlab UI binds to (port 8050).
# Default (unset) = 127.0.0.1, reachable only via an SSH tunnel.
# Set to the server's WireGuard interface IP to reach it from a VPN peer.
# SIMLAB_BIND=10.0.0.1
